import userReducer, { userSlice, setUser, removeUser } from "./user/userSlice";
import counterReducer, {
  counterSlice,
  increment,
  decrement,
  incrementByAmount,
} from "./counter/counterSlice";

export {
  userReducer,
  userSlice,
  setUser,
  removeUser,
  counterReducer,
  counterSlice,
  increment,
  decrement,
  incrementByAmount,
};
