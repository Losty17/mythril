import { User } from "../types";

const useAuth = (user?: User) => {
  // const api = useAPI();
  // const login = (loginData: LoginData): User | undefined => {
  //   // if (user) return user;
  //   // // const { data } = api.post("/auth", { ...loginData });
  //   // if (data) {
  //   //   const { email, name, avatar, token } = data;
  //   //   return { email, name, avatar, token };
  //   // }
  // };
  // return { login };
};
export default useAuth;
